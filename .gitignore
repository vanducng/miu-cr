# goreleaser build output
/dist/

# local review state (never commit reviewed code or findings)
*.db
state.db

# cached OAuth token (never commit credentials)
oauth.json

# internal planning artifacts — kept private (public repo)
.workbench/
.vd.json

# built binaries
/miucr
/miu-cr

# Astro / Starlight docs site
node_modules/
docs/dist/
.astro/

# misc
.DS_Store
*.test
*.out

# agent-local config / memory (but track the in-repo miucr skill)
.claude/*
!.claude/skills/
!.claude/skills/**

# agent skills (canonical; .claude/skills symlinks here)
.agents/*
!.agents/skills/
!.agents/skills/**
